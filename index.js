var express = require('express');var app = express();var bodyParser = require('body-parser');const axios = require('axios');const botToken = '367819622:AAGvytCTS5Kb_6SezFBnmvITnAck08XCw2w';app.use(bodyParser.json());app.use(bodyParser.urlencoded({extended: true}));app.post('/hook', function (request, response) {    const {message} = request.body;    if (!message || message.text.length == 0) {        response.end();    }    getExchangeRate(message.text)        .then(exchangeResponse => {            var chatId = message.chat.id,                formattedExchangeRate = `${message.text} ${exchangeResponse.data[0].rate} ГРН`;            return sendMessage(chatId, formattedExchangeRate);        })        .then(succes => {            console.log('Message posted')            response.end('ok')        })        .catch(err => {            console.log('Error :', err)            response.end('Error :' + err)        })});function getExchangeRate(currencyCode) {    return axios.get(`https://bank.gov.ua/NBUStatService/v1/statdirectory/exchange?valcode=${currencyCode}&json`);}function sendMessage(chatId, message) {    return axios.post(`https://api.telegram.org/bot${botToken}/sendMessage`, {        chat_id: chatId,        text: message    })}app.listen(3000, function () {    console.log('Currency Checker listening on port 3000!');});